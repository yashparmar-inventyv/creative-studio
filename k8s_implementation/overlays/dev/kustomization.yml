apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: creative-studio-dev
nameSuffix: -dev

resources:
  - ../../base

replicas:
  - name: creative-studio
    count: 2

secretGenerator:
  - name: creative-studio-secrets
    envs:
      - .env.secret

patches:
  - path: service-patch.yaml

generatorOptions:
  disableNameSuffixHash: true
